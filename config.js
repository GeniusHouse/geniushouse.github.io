/* Fill in the PostHog project key, then redeploy. Nothing else needs changing.
   The project key is a public, client-side key — it is safe in this file. */
window.ABC_CONFIG = {
  posthogKey:  "PHC_KEY_GOES_HERE",
  posthogHost: "https://us.i.posthog.com"   // EU projects: https://eu.i.posthog.com
};
